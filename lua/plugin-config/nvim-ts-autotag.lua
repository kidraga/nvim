require('nvim-ts-autotag').setup({
    disable_filetype = { "TelescopePrompt" },
    map_cr = true, -- auto indent if you hit enter inside {}
    -- more customized behaviour for brackets in the git repo
})